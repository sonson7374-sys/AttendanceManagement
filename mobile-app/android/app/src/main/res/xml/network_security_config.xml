<?xml version="1.0" encoding="utf-8"?>
<!-- 개발 환경에서 근무지관리 화면의 카카오맵 WebView가 관리자웹 Vite 개발 서버(http)를 로드하기
     위한 설정. Android WebView는 기본적으로 평문 http를 차단하므로(net::ERR_CLEARTEXT_NOT_PERMITTED)
     에뮬레이터/로컬 개발 호스트로 범위를 좁혀 허용한다. 운영 환경은 https라 영향 없음. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
